# Cloudflare Pages 配置
# 部署命令: npx wrangler pages deploy dist

name = "design-cases"
pages_build_output_dir = "dist"
compatibility_date = "2026-06-06"

# KV 绑定 - 需要在 Cloudflare 后台创建 KV 命名空间后替换下面的 ID
# [[kv_namespaces]]
# binding = "CASES_KV"
# id = "你的KV命名空间ID"
